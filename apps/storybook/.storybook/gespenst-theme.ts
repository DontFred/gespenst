import { create } from "@storybook/theming";

const GespenstTheme = create({
  base: "dark",
  brandTitle: "Gespenst",
  brandUrl: "",
  brandTarget: "_self",
  brandImage: "/logo.svg",
  fontBase: "Geist Sans, Arial",
});

export default GespenstTheme;
